[
{web_server,
	[{suites, "find <path to proj>/test/ "
		  "-name *erl "
		 },
	{tmp_dir, "<path to proj>/web_server/tmp"},
	{cmd_ct_run, "ct_run -sname testnode -no_auto_compile"
		     " -dir <path to proj>/tmp "
		     " -logdir <path to proj>/testlogs "}]
	}].
